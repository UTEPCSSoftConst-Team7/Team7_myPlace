/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShowAuthenticationDirective } from './show-authentication.directive';

describe('Directive: ShowAuthentication', () => {
  it('should create an instance', () => {
    const directive = new ShowAuthenticationDirective();
    expect(directive).toBeTruthy();
  });
});
